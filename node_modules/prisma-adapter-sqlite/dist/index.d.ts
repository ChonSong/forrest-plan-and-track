/// <reference types="node" />

import type { DatabaseSyncOptions } from 'node:sqlite';
import type { SqlDriverAdapter } from '@prisma/driver-adapter-utils';
import type { SqlMigrationAwareDriverAdapterFactory } from '@prisma/driver-adapter-utils';

export declare class PrismaSqlite implements SqlMigrationAwareDriverAdapterFactory {
    #private;
    readonly provider = "sqlite";
    readonly adapterName: string;
    constructor(config: SqliteInputParams, options?: PrismaSqliteOptions);
    connect(): Promise<SqlDriverAdapter>;
    connectToShadowDb(): Promise<SqlDriverAdapter>;
}

declare type PrismaSqliteOptions = {
    shadowDatabaseUrl?: string;
    timestampFormat?: 'iso8601' | 'unixepoch-ms';
};

declare type SqliteInputParams = DatabaseSyncOptions & {
    url: ':memory:' | (string & {});
};

export { }
